# IMPORTANT: In a real production deployment, NEVER store secrets in git.
# Use a secrets manager: HashiCorp Vault, AWS Secrets Manager, Azure Key Vault, etc.
# These values are placeholders — replace with real production credentials.
apiVersion: v1
kind: Secret
metadata:
  name: nesnes-secret
  namespace: production
  labels:
    app: nesnes
    env: production
type: Opaque
stringData:
  DB_USER: "postgres"
  DB_PASS: "prod-postgres-CHANGE-THIS-secure-pass"
  JWT_SECRET: "prod-jwt-secret-CHANGE-THIS-nesnes-key-2024"
  PGADMIN_EMAIL: "admin@admin.com"
  PGADMIN_PASS: "prod-grafana-CHANGE-THIS-pass"
